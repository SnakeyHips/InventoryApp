<Controls:MetroWindow x:Class="InventoryApp.Views.ScanDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        mc:Ignorable="d"
        ShowTitleBar="False"
        ShowMinButton="False"
        ShowMaxRestoreButton="False"
        ShowCloseButton="False"
        BorderBrush="{DynamicResource AccentColorBrush}"
        GlowBrush="{DynamicResource AccentColorBrush}"
        BorderThickness="1"
        WindowStartupLocation="CenterOwner"
        FocusManager.FocusedElement="{Binding ElementName=txtScanInput}"
        Title="Scan Reagent" Height="150" Width="300">
    <Grid Margin="15, 15, 15, 15">
        <TextBox Name="txtScanInput" Height="23" Margin="0,15,0,0" TextWrapping="Wrap" Text="" VerticalAlignment="Top" BorderBrush="{DynamicResource AccentColorBrush}"/>
        <Button Content="OK" HorizontalAlignment="Left" VerticalAlignment="Bottom" Width="75" IsDefault="True" Click="btnOK_Click"/>
        <Button Content="Cancel" HorizontalAlignment="Right" VerticalAlignment="Bottom" Width="75" IsCancel="True" Click="btnCancel_Click"/>
    </Grid>
</Controls:MetroWindow>
